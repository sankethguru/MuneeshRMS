// seed.js — empty starter data, written to data/db.json on first boot.
// A vanilla installation starts with no records. The schema (table
// structure, fields, formulas, etc.) is defined in default-schema.js
// and created separately; this file only controls the initial data rows.
// Previously this contained sample landlords, tenants, banking, property,
// and bills records — removed so a fresh install is genuinely clean.
module.exports = {};
